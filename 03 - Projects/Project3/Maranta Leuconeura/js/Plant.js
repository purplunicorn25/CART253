// Plant
//
// That class represents a plant that is growing
// On a table

class Plant {

  // constructor
  //
  // Set the initial values for the NightSky's properties
  // Either sets default values or uses the arguments provided
  constructor(x, y, width, height, startTime, avatar) {
    // Position
    this.x = x;
    this.y = y;
    // Display properties
    this.width = width;
    this.height = height;
    this.avatar = avatar;
  }

}